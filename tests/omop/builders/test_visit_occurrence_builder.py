# todo:
# baseline uses measurement date when assessment_date is null
# two assessments same date but different event_id → two visits
# duplicate (date,event_id) rows collapse to one visit
# preceding_visit_occurrence_id chains correctly after sorting

