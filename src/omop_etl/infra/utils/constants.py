# used by builders when no matching concept IDs are found for a concept,
# but we still want to create a record in that table.
# also used for subset generation of mapped concepts with Athena vocab,
# as these have no rows in the mapping files and must be provided
NO_MATCHING_CONCEPT = 0
